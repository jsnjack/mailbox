bin/
/mailbox
.monova*
*.rpm
rpmbuild/
mailbox.db
# OAuth client secrets — never commit
credentials.json
client_secret*.json
# Local model benchmark — held back entirely for now.
#
# The harness itself is clean, but bench/cases.py is not the synthetic set it
# claims to be: a 6-word-shingle comparison against the real mail it was derived
# from found 524 shared phrases, including a resignation thread, a compensation
# discussion, an own sent reply, a card's last four digits and a real flight
# route. It has to be genuinely rewritten before any of bench/ is committed.
/bench/
dist/
.claude/worktrees/
