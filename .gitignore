bin/
/mailbox
.monova*
*.rpm
rpmbuild/
mailbox.db
# OAuth client secrets — never commit
credentials.json
client_secret*.json
# Local model benchmark. The harness and the synthetic test set are committed;
# everything below is either generated output or built from real mail, and must
# never be. Before committing a change to bench/cases.py, re-run the shingle
# check against pool.json — a name scan is not sufficient and once let a whole
# corpus through.
/bench/pool.json
/bench/realcases.py
/bench/artifact.json
/bench/probe_thinking.json
/bench/prompt.py
/bench/__pycache__/
dist/
.claude/worktrees/
