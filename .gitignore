bin/
/mailbox
.monova*
*.rpm
rpmbuild/
mailbox.db
# OAuth client secrets — never commit
credentials.json
client_secret*.json
# Local model benchmark. The harness and the synthetic test set are committed;
# everything below is generated output.
#
# bench/cases.py must stay synthetic. Do not author it with real mail open: an
# earlier version was, and reproduced 524 phrases from a real mailbox while a
# name scan reported it clean. The real corpus it was checked against has since
# been deleted, so that check can no longer be re-run — which makes the rule
# "never paste real mail in", not "scrub it afterwards".
/bench/artifact.json
/bench/probe_thinking.json
/bench/prompt.py
/bench/__pycache__/
dist/
.claude/worktrees/
